package ua.footballdata.configuration;

//@Profile("mysql")
//@PropertySource(ignoreResourceNotFound = true, value = "classpath:application.properties")
public class MySQLDatasourceConfig {

	/*
	 * @Value("${spring.datasource.url}") private String datasourceUrl;
	 * 
	 * @Value("${spring.datasource.driver-class-name}") private String
	 * datasourceDriverClassName;
	 * 
	 * @Value("${spring.datasource.username}") private String datasourceUsername;
	 * 
	 * @Value("${spring.datasource.password}") private String datasourcePassword;
	 * 
	 * @Bean public DataSource datasource() { return
	 * DataSourceBuilder.create().driverClassName(datasourceDriverClassName).url(
	 * datasourceUrl)
	 * .username(datasourceUsername).password(datasourcePassword).build(); }
	 */
}
